# Copy this file to .env for local testing.
# On Railway, set these as Variables in your project settings instead —
# Railway never reads .env, but the values map 1:1.

# Required — your Anthropic API key (https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional — your Tavily API key (https://tavily.com). If left blank, the
# server still works, it just never adds live web search results.
TAVILY_API_KEY=

# Optional — shared secret required from the front end as the
# "X-Client-Secret" header. Leave blank to disable this check.
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
# Then set the SAME value in the CLIENT_SECRET constant inside
# frontend/index.html and frontend/voice.html.
CLIENT_SECRET=

# Optional — tune how many /counterpart requests one visitor can make.
# Defaults: 60 requests per 15 minutes.
RATE_LIMIT_WINDOW_MIN=15
RATE_LIMIT_MAX=60

# Optional — comma-separated list of allowed front-end origins, e.g.
#   https://my-company-counterpart.netlify.app
# Leave blank to accept requests from any origin.
ALLOWED_ORIGIN=

# Railway sets this automatically — only needed for local testing.
PORT=3000

# ── Mobile app (/api/...) — new, additive, does not affect the web app above ──

# Postgres connection string for the mobile app's problem-scoped store. On Railway,
# add a Postgres plugin to this project and it populates this automatically.
DATABASE_URL=

# Required to call POST /api/admin/keys (issues real, DB-backed access keys for the
# mobile app). Pick any long random string and keep it secret.
ADMIN_SECRET=

# Optional — connects the counterpart to real MCP servers via Anthropic's MCP
# connector, so tool calls can actually do things (send a real email, write to a
# real calendar, etc.) instead of only preparing drafts. Leave blank to disable —
# the counterpart still works exactly as before, just without any MCP tools.
# A JSON array of { "type": "url", "url", "name", "authorization_token" (optional) },
# e.g.:
#   MCP_SERVERS_JSON=[{"type":"url","url":"https://example.modelcontextprotocol.io/sse","name":"example-mcp","authorization_token":"..."}]
MCP_SERVERS_JSON=
